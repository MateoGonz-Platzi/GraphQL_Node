type Query {
  hello: String!
  getPerson(name: String, age: Int): String

  getInt: Int
  getFloat: Float
  getBoolean: Boolean
  getString: String
  getId: ID

  getNumbers(numbers: [Int!]!): [Int]
}
